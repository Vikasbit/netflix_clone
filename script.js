// Simple login form event listener
document.getElementById("loginForm").addEventListener("submit", function(e) {
    e.preventDefault(); // prevent actual form submit

    alert("Login form submitted! (This is just a demo)");
});